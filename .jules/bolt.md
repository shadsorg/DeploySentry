## 2024-04-10 - React useMemo array filtering
**Learning:** The web frontend lacks an explicit npm test command but relies on tsc for typechecking during build. We successfully optimized FlagListPage with useMemo.
**Action:** For web performance optimizations that involve React hooks, verify the rules of hooks by placing them before early returns, run linting, and verify types with tsc.
