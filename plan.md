1. Update `web/src/pages/DeploymentsPage.tsx` and `web/src/pages/FlagListPage.tsx` to hoist `search.toLowerCase()` outside of the `.filter()` function block inside `useMemo`.
2. Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
3. Submit the change.
